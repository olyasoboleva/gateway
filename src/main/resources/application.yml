server:
  port: 8080

spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
        - id: auth
          uri: http://localhost:8090
          predicates:
            - Path=/auth/signup, /auth/auth, /auth/kek
          filters:
            - StripPrefix=1